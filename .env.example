# Copy to .env and fill in. (.env is gitignored — never commit real secrets.)
DB_HOST=localhost
DB_PORT=5433
DB_NAME=ragdb
DB_USER=postgres
DB_PASSWORD=changeme

# Needed later for answer generation:
# ANTHROPIC_API_KEY=sk-ant-...
